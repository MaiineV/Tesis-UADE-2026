namespace Rollgeon.UI.HUD.CharacterFrame
{
    /// <summary>
    /// Íconos de navegación de la ruleta del marco. Se resuelven por NOMBRE del
    /// GameObject (los canónicos que autoran los installers), no por índice del
    /// array — reordenar <c>_icons</c> no rompe nada.
    /// </summary>
    public enum CharacterFrameIcon
    {
        DiceBag,
        Backpack,
        Contract,
    }
}
