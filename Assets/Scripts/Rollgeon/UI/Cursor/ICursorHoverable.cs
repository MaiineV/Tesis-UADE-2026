namespace Rollgeon.UI.Cursor
{
    /// <summary>
    /// Marcador: un objeto de mundo con collider que, al estar bajo el cursor,
    /// muestra el estado hover (enemigos, pedestales, altares, puertas). Los
    /// tiles del piso NO lo implementan — apuntar al piso es "nada".
    /// </summary>
    public interface ICursorHoverable
    {
    }
}
