---
title: TargetQueries
type: concept
domain: 04-Effects
status: historical
tags: [effects, selection, target, deprecated]
---

# Target Queries

> Deprecated. The `TQ_*` query-class pattern was replaced by
> [[SelectionSettings]] + [[ISelectionController]] (BFS over the grid,
> filtered by [[SlotState]] / [[EntityFilterMask]]).

See [[TargetRef]] for the new selection result type that effects read
from `EffectContext.SelectionResult`.
